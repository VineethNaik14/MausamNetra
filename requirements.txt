# =============================================================================
# MausamNetra — single consolidated Python requirements file
#
# Merges what used to be five separate files:
#   backend/requirements.txt        (API, DB, auth)
#   backend/requirements-ai.txt     (real AI/ML impls, optional before)
#   requirements.txt (root)         (verification service)
#   ml/classification/requirements.txt
#   ingestion_requirements.txt      (ingestion + integration workers)
#
# NOTE on versions bumped/changed during the merge (test these!):
#   - sentence-transformers pinned to 3.1.1 (was >=2.2,<3.0) -- re-run
#     ml/verification/tests/test_text_similarity.py after installing.
#   - psycopg2-binary dropped in favor of psycopg[binary] v3, which is
#     what the backend's SQLAlchemy DATABASE_URL actually uses.
# =============================================================================

# --- Web framework / API -----------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-multipart==0.0.9

# --- Database (PostgreSQL + PostGIS) -----------------------------------------
SQLAlchemy==2.0.35
alembic==1.13.3
psycopg[binary]==3.2.2
GeoAlchemy2==0.15.2

# --- Auth ----------------------------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
argon2-cffi==23.1.0

# --- HTTP client ----------------------------------------------------------------
httpx==0.27.2
requests==2.32.3

# --- Data / ML (classifier + verification engine) -----------------------------
pandas==2.2.2
numpy>=1.24,<2.0
joblib==1.4.2
scikit-learn==1.5.2
opencv-python-headless==4.10.0.84
sentence-transformers==3.1.1
torch==2.4.1
transformers==4.45.1

# --- Ingestion / streaming (Kafka producer + pipeline worker) -----------------
python-dotenv>=1.0,<2
kafka-python>=2.0,<3

# --- Testing --------------------------------------------------------------------
pytest==8.3.3
pytest-asyncio==0.24.0